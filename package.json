{
  "name": "invora-email-filter",
  "version": "1.0.0",
  "description": "Production-grade Outlook phishing and spam filter with Microsoft Graph integration",
  "private": true,
  "workspaces": [
    "backend",
    "addin"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:addin\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:addin": "npm run dev --workspace=addin",
    "build": "npm run build --workspace=backend && npm run build --workspace=addin",
    "test": "npm run test --workspace=backend && npm run test --workspace=addin",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=addin",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "seed": "npm run seed --workspace=backend",
    "reset": "npm run reset --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

